#!/usr/bin/python
#coding:utf-8
import random
print "random():", random.random()

#improt语句的另一种形式包括from关键字。之后是模块名称，import关键字和一个星号，例如 form random import *
#使用这种形式的import语句，调用random模块中的函数时不需要random前缀。但是，使用完整的名称会让代码更可读。
